# Notable API service deps. Kept minimal — FastAPI + psycopg3 async +
# httpx for the runtime/billing round-trips.
fastapi>=0.115
uvicorn[standard]>=0.32
psycopg[binary]>=3.2
httpx>=0.27
pydantic>=2.0
