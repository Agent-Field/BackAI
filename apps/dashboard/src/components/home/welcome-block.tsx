// SPDX-License-Identifier: Apache-2.0

// Welcome block placeholder.
//
// Per development/ux/pages/home.md §20, the welcome block is explicitly
// deferred from v1. This component renders nothing — the slot exists so
// the layout doesn't shift when the block is wired in a later block.

export function WelcomeBlock() {
  return null
}
