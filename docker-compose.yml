# AF Stack local development stack
#
# Brings up: Postgres, MinIO, AgentField control plane, suite runtime.
# Other services (Svix, OTel, dashboard) added as their phases land.
#
# Quickstart:
#   cp .env.example .env
#   # edit .env: set OPENROUTER_API_KEY
#   docker compose up

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: afstack
      POSTGRES_PASSWORD: afstack
      POSTGRES_MULTIPLE_DATABASES: afstack,agentfield
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./scripts/postgres-init.sh:/docker-entrypoint-initdb.d/postgres-init.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U afstack"]
      interval: 5s
      timeout: 3s
      retries: 10

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${AF_STACK_S3_ACCESS_KEY:-minio}
      MINIO_ROOT_PASSWORD: ${AF_STACK_S3_SECRET_KEY:-minio-secret}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5

  agentfield:
    image: agentfield/control-plane:latest
    environment:
      AGENTFIELD_HTTP_ADDR: 0.0.0.0:8081
      AGENTFIELD_STORAGE_MODE: postgresql
      AGENTFIELD_STORAGE_POSTGRES_URL: postgres://afstack:afstack@postgres:5432/agentfield?sslmode=disable
      AGENTFIELD_STORAGE_POSTGRES_ENABLE_MEMORY_FALLBACK: "false"
      AGENTFIELD_STORAGE_POSTGRES_ENABLE_DID_FALLBACK: "false"
      AGENTFIELD_STORAGE_POSTGRES_ENABLE_VC_FALLBACK: "false"
      AGENTFIELD_STORAGE_POSTGRES_ENABLE_AUTO_MIGRATION: "true"
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    ports:
      - "8081:8081"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8081/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Suite runtime (will be built in Phase 1). Placeholder for now.
  # runtime:
  #   build:
  #     context: .
  #     dockerfile: services/runtime/Dockerfile
  #   environment:
  #     AF_STACK_DATABASE_URL: postgres://afstack:afstack@postgres:5432/afstack?sslmode=disable
  #     AGENTFIELD_URL: http://agentfield:8081
  #   ports:
  #     - "8080:8080"
  #     - "9090:9090"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     agentfield:
  #       condition: service_healthy

  # OpenTelemetry collector (Phase 1.7). Bundled but commented until runtime ships.
  # otel-collector:
  #   image: otel/opentelemetry-collector-contrib:latest
  #   command: ["--config=/etc/otel-collector-config.yaml"]
  #   volumes:
  #     - ./deploy/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro
  #   ports:
  #     - "4317:4317"  # OTLP gRPC
  #     - "4318:4318"  # OTLP HTTP

  # Svix incoming webhook fabric (Phase 10).
  # svix:
  #   image: svix/svix-server:latest
  #   environment:
  #     WAIT_FOR: "true"
  #     SVIX_REDIS_DSN: redis://svix-redis:6379
  #     SVIX_DB_DSN: postgres://afstack:afstack@postgres:5432/svix?sslmode=disable
  #     SVIX_JWT_SECRET: ${SVIX_JWT_SECRET:-change-me-svix-secret}
  #   ports:
  #     - "8071:8071"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #
  # svix-redis:
  #   image: redis:7-alpine
  #   command: redis-server --save "" --appendonly no

  # Caddy reverse proxy + automatic TLS (production only).
  # See docker-compose.prod.yml.

volumes:
  postgres-data:
  minio-data:
